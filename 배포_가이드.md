# 수업 단계향상목표 대시보드 — 배포 가이드

강사 한 명(관리자)이 내용을 입력하면, 링크를 가진 다른 사람들은 그냥 페이지를 열어서 최신 내용을 확인할 수 있는 대시보드입니다. 별도 서버 없이 **GitHub Pages(무료 호스팅)** 위에서 작동하며, 데이터는 저장소 안의 `data.json` 파일에 저장됩니다.

## 먼저 꼭 알아두세요 (중요)

무료 GitHub Pages는 **공개(Public) 저장소**에서만 작동합니다. 즉, 배포된 사이트 주소(링크)를 아는 사람은 로그인 없이 누구나 내용을 볼 수 있습니다(검색엔진에 노출되진 않지만, 링크만 있으면 접근 가능). 수업 내용이 외부에 공개되어도 괜찮다면 문제없지만, 완전히 비공개로 운영하고 싶다면 GitHub Pro/Team 유료 플랜(비공개 저장소에서도 Pages 사용 가능)이 필요합니다. 이 가이드는 무료 방식(공개 저장소) 기준으로 안내합니다.

---

## 1단계. 새 저장소(Repository) 만들기

1. [github.com](https://github.com) 로그인 후 오른쪽 위 **+** 버튼 → **New repository** 클릭
2. Repository name에 원하는 이름 입력 (예: `lesson-dashboard`)
3. **Public** 선택 (Private로 하면 무료 Pages가 안 됩니다)
4. **Create repository** 클릭

## 2단계. 파일 업로드하기

1. 방금 만든 저장소 페이지에서 **Add file → Upload files** 클릭
2. 아래 7개 파일을 모두 드래그해서 업로드
   - `index.html`
   - `style.css`
   - `app.js`
   - `gate.js`
   - `config.js`
   - `data.json`
   - `hash-generator.html` (입장 비밀번호를 쓰실 경우에만 필요, 나중에 지워도 됨)
3. 아래쪽 **Commit changes** 클릭

## 3단계. config.js에 내 저장소 정보 입력하기

1. 저장소 페이지에서 `config.js` 파일 클릭 → 연필 아이콘(Edit) 클릭
2. 아래 두 줄을 본인 값으로 수정합니다.

```js
const GITHUB_OWNER = "YOUR_GITHUB_USERNAME";  // → 본인 깃허브 아이디로 수정
const GITHUB_REPO = "YOUR_REPO_NAME";          // → 방금 만든 저장소 이름으로 수정
```

예를 들어 깃허브 아이디가 `jasmine-edu`이고 저장소 이름이 `lesson-dashboard`라면:

```js
const GITHUB_OWNER = "jasmine-edu";
const GITHUB_REPO = "lesson-dashboard";
```

3. 오른쪽 위 **Commit changes** 클릭 (저장)

## 4단계. GitHub Pages 켜기

1. 저장소 상단 메뉴에서 **Settings** 클릭
2. 왼쪽 메뉴에서 **Pages** 클릭
3. **Branch** 항목에서 `main` 선택, 폴더는 `/ (root)` 선택 → **Save**
4. 1~2분 기다리면 화면 위쪽에 아래와 같은 배포 주소가 나타납니다.

```
https://본인아이디.github.io/저장소이름/
```

이 주소가 **모든 사람이 볼 대시보드 링크**입니다. 이 링크만 공유하면 다른 강사님들은 그냥 접속해서 보면 됩니다 (별도 가입/로그인 불필요).

## 5단계. 강사(관리자)용 개인 액세스 토큰 만들기

입력을 담당하실 강사님만 아래 토큰을 발급받아 보관하세요. 이 토큰이 있어야 "강사 모드"로 들어가 데이터를 저장할 수 있습니다.

1. GitHub 오른쪽 위 프로필 사진 → **Settings**
2. 맨 아래 왼쪽 메뉴에서 **Developer settings**
3. **Personal access tokens → Fine-grained tokens** → **Generate new token**
4. 다음과 같이 설정합니다.
   - Token name: 아무 이름 (예: `lesson-dashboard-editor`)
   - Expiration: 원하는 기간 (예: 90일, 만료되면 다시 발급하면 됩니다)
   - Repository access: **Only select repositories** → 방금 만든 저장소 선택
   - Permissions → Repository permissions → **Contents** → **Read and write** 로 설정
5. **Generate token** 클릭
6. 화면에 `github_pat_...` 로 시작하는 토큰이 딱 한 번 표시됩니다. 복사해서 안전한 곳(메모장 등)에 보관하세요. 이 화면을 벗어나면 다시 볼 수 없습니다.

⚠️ 이 토큰은 비밀번호와 같습니다. 다른 사람과 공유하지 마세요.

## 6단계. (선택) 스마트폰용 "하루씩 보기"

이제 화면 오른쪽 위에 **보기: 하루씩 / 보기: 일주일 전체** 토글 버튼이 있습니다.

- **일주일 전체**: 일/월/수/금 4개 차시를 한 표에 나란히 보여줍니다. PC 화면에 적합합니다.
- **하루씩**: 요일 탭(일/월/수/금)을 눌러 그날 내용만 세로로 길게 보여줍니다. 화면이 좁은 스마트폰에 적합합니다.

화면 너비를 기준으로 처음 접속 시 자동으로 적당한 모드가 선택되고(스마트폰이면 하루씩, PC면 일주일 전체), 한 번 버튼을 눌러 바꾸면 그 기기·브라우저에는 다음에 접속할 때도 그 설정이 기억됩니다. 별도 설정 없이 그냥 쓰시면 됩니다.

## 7단계. (선택) 입장 비밀번호 걸기

무료 GitHub Pages는 공개 저장소라서(맨 위 "먼저 꼭 알아두세요" 참고), 원하시면 아주 가벼운 "입장 비밀번호" 화면을 추가로 걸 수 있습니다.

⚠️ **먼저 꼭 이해하고 넘어가세요**: 이건 진짜 보안이 아니라 "가벼운 안내문" 수준입니다. 이 사이트는 정적 HTML 파일이라, 비밀번호를 몰라도 브라우저 개발자도구(F12)로 소스를 보거나 `data.json` 주소를 직접 열면 내용을 볼 수 있습니다. 검색엔진 노출을 막고, 링크를 실수로 보게 된 사람이나 관계없는 사람이 우연히 들어오는 것을 막는 정도의 용도로 생각해 주세요. 학생 개인정보처럼 정말 민감한 내용은 애초에 이 방식에 올리지 않는 걸 권장합니다.

그래도 걸고 싶으시면:

1. 배포된 사이트 주소 뒤에 `hash-generator.html`을 붙여서 접속합니다. (예: `https://본인아이디.github.io/저장소이름/hash-generator.html`)
2. 원하는 비밀번호를 입력하고 **해시 생성** 클릭 → 아래에 긴 영문/숫자 조합(해시값)이 나옵니다.
3. 그 값을 복사해서 저장소의 `config.js` 파일을 열고 아래처럼 붙여넣습니다.

```js
const SITE_PASSWORD_SHA256 = "여기에_복사한_해시값_붙여넣기";
```

4. **Commit changes** 클릭 → 이제 사이트에 접속하면 비밀번호를 입력해야 내용이 보입니다.
5. 비밀번호를 다시 없애고 싶으면 `SITE_PASSWORD_SHA256`을 다시 빈 문자열 `""`로 바꾸면 됩니다.

비밀번호를 한 번 맞게 입력하면 그 브라우저에는 기억되어 있어서, 매번 다시 칠 필요는 없습니다.

## 8단계. 사용 방법

**보는 사람 (다른 강사님들)**
- 배포된 링크(`https://...github.io/...`)에 접속만 하면 최신 내용이 보입니다.
- 위쪽 "새로고침" 버튼으로 최신 데이터를 다시 불러올 수 있습니다.

**입력하는 사람 (관리자 강사님)**
1. 대시보드 접속 → 오른쪽 위 **강사 모드** 클릭
2. 5단계에서 발급받은 토큰(`github_pat_...`)을 입력 → 로그인
   - 한 번 입력하면 그 브라우저에 저장되어 다음부터는 바로 강사 모드로 전환됩니다.
3. **+ 주차 추가** 버튼으로 새 주차 입력, 또는 각 주차 카드의 **수정 / 삭제** 버튼으로 기존 내용 수정
4. 요일 탭(일/월/수/금)을 클릭해 각 차시 내용을 입력
5. **저장 (GitHub에 반영)** 클릭 → 바로 GitHub 저장소의 `data.json`이 갱신되고, 페이지에도 즉시 반영됩니다. 다른 사람 화면은 그 사람이 페이지를 새로고침하면 최신 내용으로 보입니다 (보통 몇 초~1분 이내 반영).

## 문제 해결

- **"토큰이 유효하지 않거나 권한이 없습니다"** → 토큰이 만료되었거나 권한 설정이 잘못된 경우입니다. 5단계를 다시 진행해 새 토큰을 발급하세요.
- **"config.js에 GitHub 사용자명/저장소를 먼저 설정하세요"** → 3단계를 아직 안 하신 경우입니다.
- **저장은 됐는데 화면에 안 보여요** → 다른 사람 화면에서 "새로고침" 버튼을 눌러보세요. 브라우저 캐시 때문에 몇 초 정도 지연될 수 있습니다.
- **약 30주차까지 계속 추가하면?** → 그대로 "+ 주차 추가"를 반복하면 아래로 계속 쌓입니다. 데이터 양이 많아져도 문제없이 동작합니다.

## 파일 구성 (참고용)

| 파일 | 역할 |
|---|---|
| `index.html` | 페이지 뼈대 |
| `style.css` | 디자인(이미지 템플릿 스타일 반영, 모바일 화면 대응 포함) |
| `app.js` | 데이터 불러오기/화면 렌더링/저장 로직/하루씩·일주일 보기 토글 |
| `gate.js` | (선택) 입장 비밀번호 화면 로직 |
| `config.js` | 본인 GitHub 계정/저장소 정보 + (선택) 비밀번호 해시 (수정 필요) |
| `data.json` | 실제 주차별 수업 데이터가 저장되는 파일 |
| `hash-generator.html` | (선택) 비밀번호 해시 생성 도구 |
